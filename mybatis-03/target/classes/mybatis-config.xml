<?xml version="1.0" encoding="utf8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--引入外部配置文件-->
    <properties resource="db.properties"/>

    <!--为实体类起别名-->
    <typeAliases>
        <!--<typeAlias type="com.hhf.bean.User" alias="User"/>   实体类少用该方式-->
        <!--实体类多的话用下面这种， 可以直接通过扫描包来操作，想要改名字在类上使用注解-->
        <package name="com.hhf.bean"/>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis?useSSL=false&amp;useUnicode=true&amp;characterEncoding=UTF-8&amp;serverTimezone=UTC"/>
                <property name="username" value="root"/>
                <property name="password" value="0327"/>
            </dataSource>
        </environment>
        <!--当想用test这套环境的话就只用再environments里面将default改成test即可-->
        <environment id="test">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>
<!--每一个mapper都需要设置-->
    <mappers>
        <mapper resource="com/hhf/dao/UserMapper.xml"/>
<!--  方式2      <mapper class="com.hhf.dao.UserMapper"></mapper>-->
<!--  方式3      <package name="com.hhf.dao"/> -->
    </mappers>
</configuration>